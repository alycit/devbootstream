


<%= render 'shared/meta' %>


<a href="http://<%= @post.resource.identifier %>" class="avatar">
  <%= image_tag(@post.resource.profile_pic_url, :size => "64x64") %>
</a>

<div class="masthead">  
  <div class="blog_name"><%= @post.data["blog_name"] %></div>

<div class="post_title">
  <%= link_to @post.title, @post.data['short_url'], :class => "headline_link" %>  
    
  </a>
</div>  

</div>

<div class"post_body"><%= raw(@post.data['body']) %></div>

<%= link_to "original post", @post.data['short_url'], :class => "original_link" %>
