
<div class="meta">

  <div class="source_icon"><%= image_tag("#{@post.resource.source}.png", :size => "20x20") %>
  </div>

  <div class="stamp">
    <% if @post.resource.boot %>
      <b><%= @post.resource.boot.try(:name) %></b> <% cohort = @post.resource.boot.try(:cohort) %> <%= cohort.try(:name) %> &middot;
    <% end %> Posted <%= time_ago_in_words(@post.posted_at)%> ago 
  </div>


        
</div>        

